<% if @contact.valid? %>
  $('#contact-form').modal('hide');
  $('#contact-list').html("<%= escape_javascript(render(partial: 'contacts')) %>");
<% else %>
  $('#contact-form').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');
<% end %>