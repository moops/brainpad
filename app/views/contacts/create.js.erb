<% if @contact.valid? %>
  $('#modal-dialog').modal('hide');
  $('#contact-list').html("<%= escape_javascript(render(partial: 'contacts')) %>");
  addMessage('success', '<%= flash[:notice] %>');
  <% flash.discard(:notice) %>
<% else %>
  $('#modal-dialog').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');
<% end %>
