alert('show login form');

jQuery('#log-in-form')
    .html("<%= escape_javascript(render(:partial => 'sessions/form')) %>");

jQuery('#log-in-form').modal();

validate('user-session-form', {
    'email': "required",
    'password': "required"
});