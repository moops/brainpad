$('#modal-dialog').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');

var bornOnCalendar = $('#person-born-on').flatpickr({ allowInput: true });
$('#modal-dialog').on('blur', '#person-born-on', function() {
  window.setTimeout(function() { bornOnCalendar.close(); }, 250);
});
