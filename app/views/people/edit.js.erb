$('#modal_dialog').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');

var bornOnCalendar = $('#person_born_on').flatpickr({ allowInput: true });
$('#modal_dialog').on('blur', '#person_born_on', function() {
  window.setTimeout(function() { bornOnCalendar.close(); }, 50);
});
