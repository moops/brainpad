$('#modal-dialog').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');

var workoutOnCalendar = $('#workout-on').flatpickr({ allowInput: true });
$('#modal-dialog').on('blur', '#workout-on', function() {
  window.setTimeout(function() { workoutOnCalendar.close(); }, 250);
});
