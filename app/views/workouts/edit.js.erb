jQuery('#form')
    .html("<%= escape_javascript(render(:partial => 'form')) %>")
    .dialog({ autoOpen: true, width: 500, title: 'workout form', modal: true, show: 'fade' });

jQuery("input.calendar").datepicker();
jQuery("input.calendar").datepicker("option", "dateFormat", "yy-mm-dd");
jQuery("#workout_workout_on").val('<%= @workout.workout_on %>');
validate('edit_workout_' + <%= @workout.id %>);