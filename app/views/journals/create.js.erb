<% if @journal.valid? %>
  $('#journal-form').modal('hide');
  $('#journal-list').html("<%= escape_javascript(render(partial: 'journals')) %>");
<% else %>
  $('#journal-form').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');
<% end %>