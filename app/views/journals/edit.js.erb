jQuery('#form')
    .html("<%= escape_javascript(render(:partial => 'form')) %>")
    .dialog({ autoOpen: true, width: 500, title: 'journal form', modal: true, show: 'fade' });

jQuery("input.calendar").datepicker();
jQuery("input.calendar").datepicker("option", "dateFormat", "yy-mm-dd");
jQuery("#journal_entry_on").val('<%= @journal.entry_on %>');
validate('edit_journal_' + <%= @journal.id %>);