$('#modal-dialog').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');

var entryOnCalendar = $('#entry-on').flatpickr({ allowInput: true });
$('#modal-dialog').on('blur', '#entry-on', function() {
  window.setTimeout(function() { entryOnCalendar.close(); }, 250);
});
