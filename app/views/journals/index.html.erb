<!-- hidden elements -->
<div id="journal_form" class="modal fade"></div>

<%= render partial: 'shared/search_form' %>
<%= render partial: 'shared/control_buttons', locals: { new_record_path: new_journal_path } %>

<div id="journal_list" class="data-list">
  <%= render partial: 'journals' %>
</div>

<div class="tag-list">
  <%= tag_list 'journal' %>
</div>
