jQuery('#payment_form')
    .html("<%= escape_javascript(render(:partial => 'form')) %>")
    .dialog({ autoOpen: true, width: 500, title: 'payment form', modal: true, show: 'fade' });

jQuery("input.calendar").datepicker();
jQuery("input.calendar").datepicker("option", "dateFormat", "yy-mm-dd");
jQuery("#payment_payment_on").val('<%= @payment.payment_on %>');
validate('edit_payment_' + <%= @payment.id %>);