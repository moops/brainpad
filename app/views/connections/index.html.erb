<%= render partial: 'shared/data_list_header', locals: { title: 'connections', new_record_path: new_connection_path } %>

<div id="connection-list" class="data-list">
  <%= render(partial: 'connections') if @connections.present? %>
</div>

<div class="tag-list">
    <%= tag_list 'connection' %>
</div>
