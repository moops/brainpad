<% if @connection.valid? %>
  $('#connection_form').modal('hide');
  $('#connection_list').html("<%= escape_javascript(render(partial: 'connections')) %>");
<% else %>
  $('#connection_form').html("<%= escape_javascript(render(partial: 'form')) %>").modal('show');
<% end %>