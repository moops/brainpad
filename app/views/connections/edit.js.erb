jQuery('#connection_form')
    .html("<%= escape_javascript(render(:partial => 'form')) %>")
    .dialog({ autoOpen: true, width: 500, title: 'connection form', modal: true, show: 'fade' });

validate('edit_connection_' + <%= @connection.id %>);